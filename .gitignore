# dependencies
/node_modules

# next.js / build output
/.next/
/out/
*.tsbuildinfo
next-env.d.ts

# production bundle analyzer
/analyze

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# local working notes (not part of the project)
memory.md

# Claude Code: share the dev-server launch config, keep machine-specific
# settings (absolute paths, permission allowlists) local
.claude/*
!.claude/launch.json

# Generated by scripts/sync-recovery.mjs from the single tracked copy at
# Recover/ittybitz-recovery.html. Next.js only serves from public/, so the
# file is staged here at dev/build time rather than committed twice.
/public/ittybitz-recovery.html
